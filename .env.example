# Local development: copy this file to ".env"
# Railway: add these keys under Variables

# Web (frontend)
# In dev, we proxy /api to http://localhost:8080 so this can be empty.
VITE_API_BASE_URL=

# API (backend)
PORT=8080
NODE_ENV=production
OPENAI_API_KEY=

# Optional
OPENAI_MODEL=gpt-4.1-mini

